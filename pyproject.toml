[project]
name = "aiflow-chat"
version = "0.1.0"
description = "Chat application with flows, agents and MCP support"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastmcp>=2.0.0",
    "fastapi>=0.116.1",
]
