[project]
name = "gpt-js"
version = "0.1.0"
description = "HTML/JS-based chat application"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastmcp>=2.0.0",
    "fastapi>=0.116.1",
]
